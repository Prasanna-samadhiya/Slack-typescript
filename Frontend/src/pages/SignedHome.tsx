import React from 'react'

interface Props {}

function SignedHome(props: Props) {
    const {} = props

    return (
        <div>proper interface when which the user sees</div>
    )
}

export default SignedHome
